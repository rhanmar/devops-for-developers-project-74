name: push.yml
on: push

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
#      - run: cd app/
      - run: make lint-ci
        working-directory: ./app/

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
#      - run: cd app/
      - run: make lint-ci
        working-directory: ./app/

#  build-and-publish:
#    runs-on: ubuntu-latest
#    needs:
#      - lint
#      - tests
#    steps:
#      - run: ls -la
